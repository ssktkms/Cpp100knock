{

    "tasks": [
        {
          "label": "clang++ build active file",
          "type": "shell",
          "command": "clang++",
          "args": ["-std=c++17", "${fileBasename}", "-o", "${fileBasenameNoExtension}.out", "-g"],
          "group": {
            "kind": "build",
            "isDefault": true
          }
        }
      ],
    "version": "2.0.0"
}